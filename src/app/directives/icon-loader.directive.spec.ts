import { IconLoaderDirective } from './icon-loader.directive';

describe('IconLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new IconLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
